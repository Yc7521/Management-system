@page
@model RefreshRolesModel

@{
    ViewData["Title"] = "更新用户组";
}

<h1>@ViewData["Title"]</h1>
<div>
    <div class="col-md-4">
        <form asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <button type="submit" class="btn btn-primary">更新</button>
        </form>
    </div>
</div>
